language: generic

services:
  - docker

before_install:
  - echo "cloning universe"
  - git clone --single-branch -b master https://github.com/nkalband/Spark-Benchmarks-Setup.git

import: ./common.yml